Thymeleaf
1) th:text="${someObjectKey.getSomeField()}" используется для вызова функции объекта
1.1) someObjectKey - данный ключ(название может быть любое) мы добавляем в функции контроллера
model.addAttribute("key", value or object or new Object())
------------------------------------------------
2) th:href="@{/path/to/controller}" используется для указания пути запроса к контроллеру
2.1)/{id}(id=${object.getID()}) or /{id}/path(id=${object.getID()})
------------------------------------------------
3) th:field="*{someKeyWord}" используется для передачи в теле Request и для прочтения на бэкенде
------------------------------------------------
4) th:method="GET" instead of GET we can use POST, PATCH, DELETE, PUT and others
------------------------------------------------
5) th:action - Указываем путь к контроллеру
------------------------------------------------
6) th:each - что-то типа foreach
------------------------------------------------